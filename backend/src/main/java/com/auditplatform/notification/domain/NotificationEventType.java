package com.auditplatform.notification.domain;

public enum NotificationEventType {
    GENERIC,
    PASSWORD_RESET,
    CERTIFICATE_EXPIRING,
    CAPA_OVERDUE,
    COMPLAINT_OPEN,
    AUDIT_SCHEDULED
}
